{
  "name": "ar-prototype",
  "version": "1.0.0",
  "description": "MindAR image tracking + Rive UI prototype",
  "main": "dist/server.js",
  "type": "module",
  "engines": {
    "node": ">=20.12"
  },
  "scripts": {
    "build:server": "tsc",
    "build:client": "vite build",
    "build": "tsc && vite build",
    "dev:client": "vite build --watch",
    "typecheck": "tsc --noEmit && tsc --noEmit -p src/client/tsconfig.json",
    "start": "node dist/server.js",
    "test": "tsc --ignoreConfig --module nodenext --moduleResolution nodenext --target es2022 --lib es2022,dom --strict --skipLibCheck --types node --outDir .tmp-test src/client/ContentProvider.ts src/client/ContentProvider.test.ts src/client/ImageTargetAnchorSource.ts src/client/ImageTargetAnchorSource.test.ts src/client/ImageTargetLoader.ts src/client/EightWallSession.ts src/client/AnchorSource.ts src/client/TraceLog.ts src/client/types/xr8.ts src/client/types/engine-binary.d.ts packages/experience-manifest/manifest.ts && (node --test .tmp-test/src/client/ContentProvider.test.js .tmp-test/src/client/ImageTargetAnchorSource.test.js; code=$?; rm -rf .tmp-test; exit $code)"
  },
  "dependencies": {
    "@8thwall/engine-binary": "^1.0.0",
    "@rive-app/canvas": "^2.38.4",
    "express": "^5.2.1",
    "mind-ar": "1.2.5",
    "three": "0.160.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^26.1.0",
    "@types/three": "0.160.0",
    "typescript": "^6.0.3",
    "vite": "^8.1.3"
  }
}
